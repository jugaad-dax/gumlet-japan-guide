{
  "name": "gumlet-japan-guide",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "build": "npm run check && node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
