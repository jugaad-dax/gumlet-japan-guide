# 2026年9月21日 改修版の検証記録

今回の対象は、独自ドメイン向けURLの更新、広告表記の明示、紹介特典の整理、情報提供メディアへの構成変更、公式料金の反映、およびライトテーマへの刷新です。**この記録はローカルプレビューとビルド検証の結果であり、本番デプロイの結果ではありません。**

## ビルド・静的検証

`npm run build`、`npx --yes html-validate --rule void-style:off index.html`、`node --check script.js`、`git diff --check`が成功しました。HTML検証の`void-style`例外は、依頼者から指定されたSearch Consoleの自己終了形式のmetaタグを維持するためです。ビルドは公開用7ファイルを`dist/`に生成します。

| 対象 | 確認結果 |
|---|---|
| URL | canonical・OGP・Twitterカード・JSON-LDが`https://site-speedup.com/`を参照。 |
| PR表示 | 本文より前の最上部にPRと広告開示文を表示。 |
| 紹介特典 | 5か所のCTAに統一表現と自動適用・コード不要の注記を設置。 |
| 個別対応への誘導 | 窓口案内・該当リンク・伴走表現・JSON-LDのContactPointを撤去。 |
| FAQ・手順 | 表示中の回答・3ステップと構造化データが一致。 |
| Googleタグ | 認証値、GA4測定ID、ローダーとインラインコードを維持。CSPハッシュ一致。 |
| 構造化データ | 7種類のJSON-LDを解析でき、記事日付も表示と一致。 |
| 装飾 | グリッド・グロー・グラデーション・旧AI解説画像を撤去。 |
| OGP | 1200×630のPNGを生成し、元のSVGも保持。 |

## ブラウザ検証

Chromium / Playwrightで320・375・390・430・768・1024・1440 CSS pxを検証し、各画面幅と文書幅が一致しました。比較表の横スクロールは専用コンテナ内に限定されています。モバイルメニューの開閉とEscapeキー、FAQの展開が動作し、未処理のページ例外はありませんでした。

375×812ではトッププレイヤーが縦位置453.84〜645.66pxに入り、スクロール前から見えます。1440×960では317.34〜660.48pxに表示されます。375・430・1440pxで、実動画の`readyState=4`、正の再生時間、`paused=false`、動画エラーなしを確認しました。動画の内容が表示された状態のPCスクリーンショットと、料金比較表も目視確認しています。

ローカル検証では`_headers`のCSPを適用しました。テストアクセスが本番GA4へ混入しないよう、ブラウザテスト側でGoogleの外部通信を止めています。サイトコードのGA4設定は変更していません。

## 価格と注記

公式情報の確認日は2026年9月21日です。VimeoはStandard $25／Advanced $75、GumletはGrowth $19／Business $99を掲載し、いずれも年払い時の月額相当と明記しました。CloudFrontの$0.085/GBは米国・欧州の例として扱い、日本の該当帯域単価$0.114/GBを併記しました。1米ドル＝156.8円の換算は`bc`で計算し、端数を参考値として丸めています。

## 検証対象外

本番へのプッシュ・デプロイ、DNS・TLS・カスタムドメイン設定、Search Consoleの新ドメイン認証、GA4管理画面の設定変更は実施していません。割引の自動適用は運営者提供の仕様を表示したもので、購入による適用実証ではありません。法令への適合を保証する法的審査、外部LLMOツールの再採点、実機全ブラウザ試験、長時間再生試験も対象外です。
